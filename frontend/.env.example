# Base URL for backend API calls. Leave unset to use the Vite dev proxy (/api).
# VITE_API_BASE_URL=http://localhost:8000

# Use in-memory mocks instead of the real backend (Phase 1). Default: true.
VITE_USE_MOCKS=true

/**
 * In-memory fakes that replay the Claude Design prototype's behaviour
 * (staged pipeline timings, canned Q&A answers) so the UI runs end-to-end
 * with no backend. Built out in Phase 1 from the logic in
 * design-reference/Manual Assistant.dc.html.
 */
export {};
